{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "Rhythm/Schemas/bezier-data-point.schema.json",
  "title": "BezierDataPoint",
  "description": "A point on a bezier curve with two anchors that affects the curve's interpolation when translated to the x-y coordinate game space.",
  "type": "object",
  "properties": {
    "origin": {
      "description": "A terminating or extreme point when rendering a bezier curve",
      "$ref": "tu-coord.schema.json"
    },
    "anchor1": {
      "description": "A terminating or extreme point when rendering a bezier curve",
      "$ref": "tu-coord.schema.json"
    }
  },
  "required": ["origin"]
}
